using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class SealedEffect : MonoBehaviour
{
    private Animator animator;

    // 나중에 애니메이션 들어가야함.
    private void Start()
    {
        animator = GetComponent<Animator>();
        if(animator != null)
        {

        }
    }
}
